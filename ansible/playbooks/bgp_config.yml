---
# Exercise: Configure iBGP on lab routers
# Reference: ansible/playbooks/route_audit.yml (the completed example)
#
# YOUR TASK:
# 1. Define the play targeting 'core' and 'edge' host groups
# 2. Set vars: bgp_asn (65000), aws_asn (64512)
# 3. Create tasks to:
#    a. Configure BGP process and router-id on each device
#    b. Configure iBGP neighbors (core as route reflector to edge routers)
#    c. Configure edge routers to peer with core's loopback
#    d. Activate IPv4 address family for all neighbors
#    e. Save the configuration
#
# HINTS:
# - Use cisco.ios.ios_config module for pushing config lines
# - Use 'when: bgp_role == "route_reflector"' to conditionally apply RR config
# - Reference hostvars[item].router_id to get neighbor IPs from inventory
# - Use 'loop: "{{ groups["edge"] }}"' to iterate over edge routers
#
# TEST: After running, verify with:
#   ansible -m cisco.ios.ios_command -a "commands='show ip bgp summary'" all
#
# DOCS:
# - https://docs.ansible.com/ansible/latest/collections/cisco/ios/ios_config_module.html
# - https://docs.ansible.com/ansible/latest/collections/cisco/ios/ios_command_module.html

- name: Configure iBGP on core and edge routers
  hosts: # TODO: which host groups?
  gather_facts: false

  vars:
    # TODO: define your BGP ASNs here

  tasks:
    # TODO: write your tasks here
    - name: Configure BGP process
      debug:
        msg: "Replace this with cisco.ios.ios_config"
