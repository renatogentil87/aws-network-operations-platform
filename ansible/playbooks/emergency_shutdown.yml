---
# Exercise: Emergency BGP Peer Shutdown
# This playbook is called by the Python remediator when BGP flap threshold is exceeded.
#
# YOUR TASK:
# 1. Accept extra vars: target_host, neighbor_ip (passed with -e flag)
# 2. Target the specific host using "{{ target_host }}"
# 3. Create tasks to:
#    a. Shutdown the BGP neighbor using ios_config
#    b. Log the action (use debug module)
#    c. Save the config
#    d. Append a line to ../outputs/remediation_log.txt with timestamp + details
#
# USAGE:
#   ansible-playbook playbooks/emergency_shutdown.yml \
#     -i inventory/gns3_lab.yml \
#     -e "target_host=router-edge1 neighbor_ip=169.254.100.2"
#
# HINTS:
# - The IOS command to shutdown a BGP peer:
#     router bgp 65000
#      neighbor <IP> shutdown
# - Use delegate_to: localhost + lineinfile module to write the log file
# - Use lookup('pipe', 'date -u +%Y-%m-%dT%H:%M:%SZ') for timestamp

- name: Emergency shutdown of BGP peer
  hosts: # TODO: use the variable
  gather_facts: false

  tasks:
    # TODO: write your tasks
    - name: Shutdown BGP neighbor
      debug:
        msg: "Replace this with cisco.ios.ios_config to shutdown the peer"
